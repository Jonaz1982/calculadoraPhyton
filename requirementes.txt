trigger:
  branches:
    include:
      - main

pool:
  vmImage: "ubuntu-lastest"

steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: "3X"
      addToPath: true

  - script: |
      python -m pip install --upgrade pip
    displayName: "Instalar pip"

  - script: |
      pip install -r requirementes.txt
    displayName: "Instalar dependecias"

  - script: |
      python test/test_calculadora.py
    displayName: "Ejecutar pruebas"
